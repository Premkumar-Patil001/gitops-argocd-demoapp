apiVersion: v1
kind: Service
metadata:
  name: gitops-demo-service
  namespace: default
  labels:
    app: gitops-demo-app
    managed-by: argocd

spec:
  selector:
    app: gitops-demo-app

  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 5000

  type: NodePort
